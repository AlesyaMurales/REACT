

const initialState = {
    messages: [
        {
            id: 1,
            title: 'Who you'
        }
    ]
}

export const messagesReducer = (state = initialState, action) => {
    switch (action.type) {



        default:
            return state
    }
}